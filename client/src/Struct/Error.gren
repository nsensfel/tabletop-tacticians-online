module Struct.Error exposing
   (
      Category(..),
      Type,
      new,
      get_category,
      get_message,
      category_from_text
   )

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type Category =
   Warning
   | MissingInput
   | Error
   | Network
   | Programming

type alias Type =
   {
      category : Category,
      message : String
   }

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
category_from_text : String -> Category
category_from_text name =
   when name is
      "wrn" -> Warning
      "err" -> Error
      "net" -> Network
      "pog" -> Programming
      "msi" -> MissingInput
      _ -> Programming

new : Category -> String -> Type
new category message = { category = category, message = message }

get_category : Type -> Category
get_category error = error.category

get_message : Type -> String
get_message error = error.message
