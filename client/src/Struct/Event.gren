module Struct.Event exposing (Type(..), to_string)

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Browser

import Url

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Error
import Struct.Session

import Struct.Page.EditProfile
import Struct.Page.Game
import Struct.Page.GameList
import Struct.Page.Login
import Struct.Page.Register

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type Type =
   None
   | Sequence (Array Type)
   | LoginPage (Struct.Page.Login.Event)
   | RegisterPage (Struct.Page.Register.Event)
   | EditProfilePage (Struct.Page.EditProfile.Event)
   | GameListPage (Struct.Page.GameList.Event)
   | GamePage (Struct.Page.Game.Event)
   | SetSession Struct.Session.Type
   | EraseSession
   | PageURLChangedTo Url.Url
   | PageURLRequested Browser.UrlRequest
   | UnknownMessageTypeReceived String
   | ErrorOccurred Struct.Error.Type
   | Confirm

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
to_string : Type -> String
to_string event =
   when event is
      None -> "None"
      (Sequence seq) -> ("(Sequence: " ++ "..." ++ ")")
      (LoginPage login_event) ->
         (
            "(Login Event: "
            ++ (Struct.Page.Login.event_to_string login_event)
            ++ ")"
         )

      (RegisterPage register_event) ->
         (
            "(Register Event: "
            ++ (Struct.Page.Register.event_to_string register_event)
            ++ ")"
         )

      (EditProfilePage edit_profile_event) ->
         (
            "(EditProfile Event: "
            ++ (Struct.Page.EditProfile.event_to_string edit_profile_event)
            ++ ")"
         )

      (GameListPage game_list_event) ->
         (
            "(GameList Event: "
            ++ (Struct.Page.GameList.event_to_string game_list_event)
            ++ ")"
         )

      (GamePage game_event) ->
         (
            "(Game Event: "
            ++ (Struct.Page.Game.event_to_string game_event)
            ++ ")"
         )

      (SetSession _) -> "SetSession"
      EraseSession -> "EraseSession"
      (PageURLChangedTo _) -> "PageURLChangedTo"
      (PageURLRequested _) -> "PageURLRequested"
      (UnknownMessageTypeReceived str) ->
         ("UnknownMessageTypeReceived: " ++ str)

      (ErrorOccurred _) -> "ErrorOccurred"
      Confirm -> "Confirm"
