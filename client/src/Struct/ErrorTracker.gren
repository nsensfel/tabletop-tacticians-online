module Struct.ErrorTracker exposing
   (
      Type,
      new,
      get_unread_errors,
      get_read_errors,
      mark_errors_as_read,
      push
   )

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import List

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Error

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type alias Type =
   {
      unread : (List Struct.Error.Type),
      read : (List Struct.Error.Type)
   }

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
new : Type
new = { unread = [], read = [] }

get_unread_errors : Type -> (List Struct.Error.Type)
get_unread_errors tracker = tracker.unread

get_read_errors : Type -> (List Struct.Error.Type)
get_read_errors tracker = tracker.read

mark_errors_as_read : Type -> Type
mark_errors_as_read tracker =
   {tracker |
      read = [],
      unread = (List.append tracker.read tracker.unread)
   }

push : Struct.Error.Type -> Type -> Type
push error tracker = {tracker | read = (error :: tracker.read) }
