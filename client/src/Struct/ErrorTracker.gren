module Struct.ErrorTracker exposing
   (
      Type,
      new,
      get_unread_errors,
      get_read_errors,
      mark_errors_as_read,
      push
   )

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Array

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Error

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type alias Type =
   {
      unread : (Array Struct.Error.Type),
      read : (Array Struct.Error.Type)
   }

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
new : Type
new = { unread = [], read = [] }

get_unread_errors : Type -> (Array Struct.Error.Type)
get_unread_errors tracker = tracker.unread

get_read_errors : Type -> (Array Struct.Error.Type)
get_read_errors tracker = tracker.read

mark_errors_as_read : Type -> Type
mark_errors_as_read tracker =
   {tracker |
      read = [],
      unread = (Array.append tracker.read tracker.unread)
   }

push : Struct.Error.Type -> Type -> Type
push error tracker = {tracker | unread = (Array.append [error] tracker.unread) }
