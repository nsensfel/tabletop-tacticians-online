module Struct.Page exposing (Type(..), new)

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Url

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Page.EditProfile
import Struct.Page.Game
import Struct.Page.GameList
import Struct.Page.Login
import Struct.Page.Register

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type Type =
   Home
   | EditProfile Struct.Page.EditProfile.Type
   | Game Struct.Page.Game.Type
   | GameList Struct.Page.GameList.Type
   | Login Struct.Page.Login.Type
   | Register Struct.Page.Register.Type
   | Unknown String

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
new : Url.Url -> Type
new url =
   when url.path is
      "/" -> Home
      "/login" -> (Login (Struct.Page.Login.new))
      "/register" -> (Register (Struct.Page.Register.new))
      "/edit_profile" -> (EditProfile (Struct.Page.EditProfile.new))
      "/game_list" -> (GameList (Struct.Page.GameList.new))
      "/game" -> (Game (Struct.Page.Game.new))
      other -> (Unknown other)
