module Struct.Page.GameList exposing
   (
      Type,
      Entry,
      Event(..),
      new,
      get_list,
      set_list,
      event_to_string
   )

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type alias Entry =
   {
      id : String
   }

type alias Type =
   {
      list : (Array Entry)
   }

type Event =
   EnterGame String
   | LeaveGame String

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
event_to_string : Event -> String
event_to_string event =
   when event is
      (EnterGame _) -> "EnterGame"
      | (LeaveGame _) -> "LeaveGame"

new : Type
new = { list = [] }

get_list : Type -> (Array Entry)
get_list game_list = game_list.list

set_list : (Array Entry) -> Type -> Type
set_list list game_list = {game_list | list = list}
