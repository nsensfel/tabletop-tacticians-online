module Struct.Page.Room.Action.Shuffle exposing
   (
      Type,
      get_deck_id,
      get_previous_top,
      get_new_top,
      decoder
   )

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Json.Decode

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Comm.Protocol

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type alias Type =
   {
      deck_id : String,
      previous_top : String,
      new_top : String
   }

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
get_deck_id : String
get_deck_id shuffle = shuffle.deck_id

get_new_top : String
get_new_top shuffle = shuffle.new_top

get_previous_top : String
get_previous_top shuffle = shuffle.previous_top

decoder : (Json.Decode.Decoder Type)
decoder =
   (Json.Decode.map3
      ( \deck_id previous_top new_top ->
         {
            deck_id = deck_id,
            previous_top = previous_top,
            new_top = new_top
         }
      )
      (Json.Decode.field Comm.Protocol.deck_id_field Json.Decode.string)
      (Json.Decode.field Comm.Protocol.previous_top_field Json.Decode.string)
      (Json.Decode.field Comm.Protocol.new_top_field Json.Decode.string)
   )
