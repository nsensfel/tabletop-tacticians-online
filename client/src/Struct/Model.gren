module Struct.Model exposing
   (
      Type,
      init,
      get_page,
      set_page,
      get_navigation_key
   )

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Browser.Navigation

import Url

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Page

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type alias Type =
   {
      page : Struct.Page.Type,
      nav_key : Browser.Navigation.Key
   }

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
init : Url.Url -> Browser.Navigation.Key -> Type
init url nav_key =
   {
      page = (Struct.Page.from_url url),
      nav_key = nav_key
   }

get_page : Type -> Struct.Page.Type
get_page model = model.page

set_page : Struct.Page.Type -> Type -> Type
set_page page model = {model | page = page}

get_navigation_key : Type -> Browser.Navigation.Key
get_navigation_key model = model.nav_key
