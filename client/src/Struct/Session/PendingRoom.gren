module Struct.Session.PendingRoom exposing
   (
      Type,
      decoder,
      get_id,
      get_game_id,
      get_name
   )

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Json.Decode

-- Third Party -----------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Comm.Protocol

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type alias Type =
   {
      id : String,
      game_id : String,
      name : String
   }

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
get_id : Type -> String
get_id pending_room = pending_room.id

get_game_id : Type -> String
get_game_id pending_room = pending_room.game_id

get_name : Type -> String
get_name pending_room = pending_room.name

decoder : (Json.Decode.Decoder Type)
decoder =
   (Json.Decode.map3
      ( \id game_id name ->
         {
            id = id,
            game_id = game_id,
            name = name
         }
      )
      (Json.Decode.field Comm.Protocol.room_id_field Json.Decode.string)
      (Json.Decode.field Comm.Protocol.room_game_id_field Json.Decode.string)
      (Json.Decode.field Comm.Protocol.room_name_field Json.Decode.string)
   )
