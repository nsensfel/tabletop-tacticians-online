module View.Game.Card exposing (get_html)

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Html
import Html.Attributes
import Html.Events

import String

-- Third Party------------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Model
import Struct.Event

import Struct.Page.Game
import Struct.Page.Game.Card
import Struct.Page.Game.Attitude

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------
int_to_px : Int -> String
int_to_px i = ((String.fromInt i) ++ "px")

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
get_html : Struct.Page.Game.Card.Type -> (Html.Html Struct.Event.Type)
get_html card =
   let
      attitude = (Struct.Page.Game.Card.get_attitude card)
   in
      (Html.img
         [
            (Html.Attributes.class "game-card"),
            (Html.Attributes.style
               "width"
               (int_to_px (Struct.Page.Game.Card.get_width card))
            ),
            (Html.Attributes.style
               "height"
               (int_to_px (Struct.Page.Game.Card.get_height card))
            ),
            (Html.Attributes.style
               "top"
               (int_to_px (Struct.Page.Game.Attitude.get_y attitude))
            ),
            (Html.Attributes.style
               "left"
               (int_to_px (Struct.Page.Game.Attitude.get_x attitude))
            ),
            (Html.Attributes.style
               "z-index"
               (String.fromInt (Struct.Page.Game.Attitude.get_z attitude))
            ),
            (Html.Attributes.style
               "transform"
               (
                  "rotate("
                  ++
                  (String.fromInt
                     (Struct.Page.Game.Attitude.get_rotation attitude)
                  )
                  ++ "deg)"
               )
            ),
            (Html.Attributes.hidden
               (not (Struct.Page.Game.Card.get_is_displayed card))
            ),
            (Html.Attributes.src
               (Struct.Page.Game.Card.get_displayed_image card)
            ),
            (Html.Events.onClick
               (Struct.Event.GamePage
                  (Struct.Page.Game.SelectCard
                     (Struct.Page.Game.Card.get_id card)
                  )
               )
            )
         ]
         [
         ]
      )
