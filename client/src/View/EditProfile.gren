module View.EditProfile exposing (view)

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Browser

import Html
import Html.Attributes
import Html.Events

import String

-- Third Party------------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Model
import Struct.Event

import Struct.Page.EditProfile

import View.ErrorTracker
--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------
new_password_field :
   (
      Struct.Page.EditProfile.Type ->
      (Html.Html Struct.Event.Type)
   )
new_password_field edit_profile =
   let
      new_password = (Struct.Page.EditProfile.get_new_password edit_profile)
   in
      (Html.div
         [
            (Html.Attributes.class "user-input"),
            (Html.Attributes.class "edit-profile-new-password")
         ]
         [
            (Html.h1 [] [ (Html.text "New Password") ]),
            (Html.input
               [
                  (Html.Events.onInput
                     ( \new_new_password ->
                        (Struct.Event.EditProfilePage
                           (Struct.Page.EditProfile.SetNewPasswordTo
                              new_new_password
                           )
                        )
                     )
                  ),
                  (Html.Attributes.value new_password),
                  if (String.isEmpty new_password)
                  then (Html.Attributes.class "empty-input")
                  else (Html.Attributes.class "")
               ]
               [
               ]
            )
         ]
      )

password_field : Struct.Page.EditProfile.Type -> (Html.Html Struct.Event.Type)
password_field edit_profile =
   let
      password = (Struct.Page.EditProfile.get_password edit_profile)
   in
      (Html.div
         [
            (Html.Attributes.class "user-input"),
            (Html.Attributes.class "edit-profile-password")
         ]
         [
            (Html.h1 [] [ (Html.text "Password") ]),
            (Html.input
               [
                  (Html.Events.onInput
                     ( \new_password ->
                        (Struct.Event.EditProfilePage
                           (Struct.Page.EditProfile.SetPasswordTo new_password)
                        )
                     )
                  ),
                  (Html.Attributes.value password),
                  if (String.isEmpty password)
                  then (Html.Attributes.class "empty-input")
                  else (Html.Attributes.class "")
               ]
               [
               ]
            )
         ]
      )

new_password_confirmation_field : Struct.Page.EditProfile.Type -> (Html.Html Struct.Event.Type)
new_password_confirmation_field edit_profile =
   let
      new_password = (Struct.Page.EditProfile.get_new_password edit_profile)
      new_password_confirmation =
         (Struct.Page.EditProfile.get_new_password_confirmation edit_profile)
   in
      (Html.div
         [
            (Html.Attributes.class "user-input"),
            (Html.Attributes.class "edit-profile-new-password-confirmation")
         ]
         [
            (Html.h1 [] [ (Html.text "New Password Confirmation") ]),
            (Html.input
               [
                  (Html.Events.onInput
                     ( \new_password_confirmation ->
                        (Struct.Event.EditProfilePage
                           (Struct.Page.EditProfile.SetNewPasswordConfirmationTo
                              new_password_confirmation
                           )
                        )
                     )
                  ),
                  (Html.Attributes.value password_confirmation),
                  if (String.isEmpty password_confirmation)
                  then (Html.Attributes.class "empty-input")
                  else (Html.Attributes.class ""),
                  if (new_password == new_password_confirmation)
                  then (Html.Attributes.class "validated-input")
                  else (Html.Attributes.class "erroneous-input")
               ]
               [
               ]
            )
         ]
      )

displayed_name_field : Struct.Page.EditProfile.Type -> (Html.Html Struct.Event.Type)
displayed_name_field edit_profile =
   let
      displayed_name = (Struct.Page.EditProfile.get_displayed_name edit_profile)
   in
      (Html.div
         [
            (Html.Attributes.class "user-input"),
            (Html.Attributes.class "edit-profile-displayed_name")
         ]
         [
            (Html.h1 [] [ (Html.text "DisplayedName") ]),
            (Html.input
               [
                  (Html.Events.onInput
                     ( \new_displayed_name ->
                        (Struct.Event.EditProfilePage
                           (Struct.Page.EditProfile.SetDisplayedNameTo
                              new_displayed_name
                           )
                        )
                     )
                  ),
                  (Html.Attributes.value displayed_name),
                  if (String.isEmpty displayed_name)
                  then (Html.Attributes.class "empty-input")
                  else (Html.Attributes.class "")
               ]
               [
               ]
            )
         ]
      )

email_field : Struct.Page.EditProfile.Type -> (Html.Html Struct.Event.Type)
email_field edit_profile =
   let
      email = (Struct.Page.EditProfile.get_email edit_profile)
   in
      (Html.div
         [
            (Html.Attributes.class "user-input"),
            (Html.Attributes.class "edit-profile-email")
         ]
         [
            (Html.h1 [] [ (Html.text "Email") ]),
            (Html.input
               [
                  (Html.Events.onInput
                     ( \new_email ->
                        (Struct.Event.EditProfilePage
                           (Struct.Page.EditProfile.SetEmailTo new_email)
                        )
                     )
                  ),
                  (Html.Attributes.value email),
                  if (String.isEmpty email)
                  then (Html.Attributes.class "empty-input")
                  else (Html.Attributes.class "")
               ]
               [
               ]
            )
         ]
      )

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
view :
   (
      Struct.Model.Type ->
      Struct.Page.EditProfile.Type ->
      (Browser.Document Struct.Event.Type)
   )
view model edit_profile =
   {
      title = "Edit Profile",
      body =
      [
         (Html.article
            []
            [
               (password_field edit_profile),
               (new_password_field edit_profile),
               (new_password_confirmation_field edit_profile),
               (displayed_name_field edit_profile),
               (email_field edit_profile),
               (Html.button
                  [
                     (Html.Events.onClick
                        (Struct.Event.EditProfilePage
                           Struct.Page.EditProfile.FormSubmitted
                        )
                     )
                  ]
                  [
                     (Html.text "Send")
                  ]
               )
            ]
         ),
         (View.ErrorTracker.get_html model)
      ]
   }
