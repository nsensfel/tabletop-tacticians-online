module View.Page.Room.Deck exposing (get_html)

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Html
import Html.Attributes
import Html.Events

import String

-- Third Party------------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Model
import Struct.Event

import Struct.Page.Room
import Struct.Page.Room.Deck
import Struct.Page.Room.Attitude

import Dragoman.RoomObjectDeck

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
get_html :
   (
      (Array (Html.Attribute Struct.Event.Type)) ->
      Dragoman.RoomObjectDeck.Type ->
      (Html.Html Struct.Event.Type)
   )
get_html attributes deck =
   let size = (Array.length (Dragoman.RoomObjectDeck.get_contents deck)) in
   (Html.img
      (
         attributes
         ++
         [
            (Html.Attributes.class "room-deck"),
            (
               if (size == 0)
               then (Html.Attributes.class "room-deck-empty")
               else (Html.Attributes.class "")
            ),
            (Html.Attributes.src (Dragoman.RoomObjectDeck.get_url deck))
         ]
      )
      [
         (Html.text (String.fromInt size))
      ]
   )
