module Module.Init exposing (init)

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------

-- Standard Library ------------------------------------------------------------
import Debug

-- Third Party------------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Event
import Struct.Model
import Struct.InvocationParameters

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
init :
   (
      Struct.InvocationParameters.Encoded ->
      { model : Struct.Model.Type, command : (Cmd Struct.Event.Type) }
   )
init flags =
   let
      invocation_parameters =
         (Struct.InvocationParameters.new
            (Debug.log "flags:" flags)
         )
      model = (Struct.Model.new invocation_parameters)
   in
      when (Struct.Model.get_page model) is
         -- TODO: request data depending on page.
         _ -> -- Nothing else to do
            {
               model = model,
               command = (Cmd.none)
            }
