port module Main exposing (main)

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Browser

-- Third Party------------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Module.Init
import Module.Subscriptions
import Module.Update
import Module.View

import Struct.Event

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------
port set_page_title : String -> Cmd msg
port url_param_read_all : Cmd msg
port url_param_read : String -> Cmd msg
port url_param_write : String -> Cmd msg
port url_param_delete : String -> Cmd msg
port url_param_delete_all : Cmd msg
port local_read_all : Cmd msg
port local_read : String -> Cmd msg
port local_write : String -> Cmd msg
port local_delete : String -> Cmd msg
port local_delete_all : Cmd msg

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
main =
   (Browser.application
      {
         init = Module.Init.init,
         update = Module.Update.update,
         view = Module.View.view,
         subscriptions = Module.Subscriptions.subscriptions,
         onUrlRequest = Struct.Event.PageURLRequested,
         onUrlChange = Struct.Event.PageURLChangedTo
      }
   )
