module Comm.Send exposing (send, Destination(..))

--------------------------------------------------------------------------------
-- IMPORTS ---------------------------------------------------------------------
--------------------------------------------------------------------------------
-- Standard Library ------------------------------------------------------------
import Json.Encode

import Http

-- Third Party------------------------------------------------------------------

-- Project ---------------------------------------------------------------------
import Struct.Event
import Struct.Model

import Comm.Receive

import Parameters

--------------------------------------------------------------------------------
-- TYPES -----------------------------------------------------------------------
--------------------------------------------------------------------------------
type Destination =
   LoginHandler
   | RegisterHandler
   | EditProfileHandler
   | RoomListHandler
   | RoomHandler

--------------------------------------------------------------------------------
-- LOCAL FUNCTIONS -------------------------------------------------------------
--------------------------------------------------------------------------------
destination_to_url : Destination -> String
destination_to_url destination =
   (
      (Parameters.request_handler_url)
      ++
      (
         when destination is
            LoginHandler -> "/login/"
            RegisterHandler -> "/register/"
            EditProfileHandler -> "/edit_profile/"
            RoomListHandler -> "/room_list/"
            RoomHandler -> "/room/"
      )
   )

--------------------------------------------------------------------------------
-- EXPORTED FUNCTIONS ----------------------------------------------------------
--------------------------------------------------------------------------------
send : Json.Encode.Value -> Destination -> (Cmd Struct.Event.Type)
send json_body destination =
   (Http.post
      {
         url = (destination_to_url destination),
         body = (Http.jsonBody json_body),
         expect = Comm.Receive.expect
      }
   )
