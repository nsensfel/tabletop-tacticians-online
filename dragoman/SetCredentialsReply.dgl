(require ServerReplyID)
(require Credentials)
(require PendingRoom)

(object Set_Credentials_Reply
	(entry message_id msg Server_Reply_ID (const set_credentials))
	(entry pending_rooms (dict id Pending_Room))
	(entry credentials Credentials)
)
