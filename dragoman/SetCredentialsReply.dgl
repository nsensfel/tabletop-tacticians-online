(require ServerReplyID)
(require Credentials)
(require PendingRoom)

(object SetCredentialsReply
	(entry message_id msg ServerReplyID (const SetCredentials))
	(entry pending_rooms (dict id PendingRoom))
	(entry credentials Credentials)
)
