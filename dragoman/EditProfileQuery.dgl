(require Credentials)

(object Edit_Profile_Query
	(entry credentials Credentials)
	(entry password string)
	(entry new_password string)
	(entry displayed_name string)
	(entry avatar string)
	(entry email string)
)
